eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8761/eureka/"

microservice:
  payment-service:
    endpoints:
      endpoint: http://PAYMENT-SERVICE/payment/do-payment

  order-service:
    endpoints:
      endpoint: http://ORDER-SERVICE/order/book-order
      
